
# Transaction Management Lab — Minimal Project

This folder has been cleaned to keep only a single static entry file for quick inspection.

- Kept: `index.html`
- Removed: build/config/source files (React, Vite, TypeScript, node modules, etc.)

Quick start (view locally):

1. Open `index.html` in a browser (double-click) — or serve it with a simple static server:

```bash
# Python 3
python3 -m http.server 8000
# then visit http://localhost:8000/index.html
```

Notes for contributors:

- This is intentionally minimal. If you want to restore a full React/Vite project, reinitialize with `npm init` or `yarn create vite` and re-add source files.
- Add any additional assets or configs under a new branch or folder; keep `index.html` as the single-authoritative entry for this lab.


---

Generated on: 2026-01-07
